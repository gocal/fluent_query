name: basic
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  fluent_query:

dev_dependencies:
  build_runner:
  fluent_query_generator:
  flutter_test:
    sdk: flutter

dependency_overrides:
  fluent_query:
    path: ../../fluent_query
  fluent_query_generator:
    path: ../../fluent_query_generator

flutter:
  uses-material-design: true
